for (let i = 2; i < 34; i += 1) {
  const rounds = Math.ceil(Math.log2(i)) + Math.ceil(Math.log2(Math.log2(i)));
  const placing = 
  console.log(`entrants:${i}`);
  console.log(`rounds: ${rounds}\n`);
  console.log()
}
